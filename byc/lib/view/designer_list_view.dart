import 'package:flutter/material.dart';

class DesignerListView extends StatelessWidget {
  const DesignerListView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("DesignerListView"),
      ),
    );
  }
}
