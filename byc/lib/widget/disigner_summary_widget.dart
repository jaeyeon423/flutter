import 'package:flutter/material.dart';

class DesignerSummaryWidget extends StatelessWidget {
  const DesignerSummaryWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
