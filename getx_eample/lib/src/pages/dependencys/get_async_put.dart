import 'package:flutter/material.dart';

class GetAsyncPut extends StatelessWidget {
  const GetAsyncPut({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Get Async Put"),
      ),
    );
  }
}
